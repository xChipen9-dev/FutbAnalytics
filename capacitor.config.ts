import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'centro-inteligencia-ninja',
  webDir: 'www'
};

export default config;
